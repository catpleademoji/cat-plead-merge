export type CatAssetData = {
  name: string;
  src: string;
  size: number;
  score: number;
}

export type ParticleAssetData = {
  name: string;
  src: string;
  size: number;
}

export type SoundEffectAssetData = {
  name: string;
  src: string[];
}
